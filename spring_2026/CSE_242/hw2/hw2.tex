\documentclass{article}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{textcomp}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{graphics}
\usepackage{pdfpages}

\usepackage{amsthm}
\newtheorem{theorem}{Theorem}[section]

\newtheorem{lemma}[theorem]{Lemma}



\newcommand{\coNP}{\textrm{co-}\mathbb{NP}}

\title{cse 242 hw2}
\author{Ian Chi}
\date{\today}
\begin{document}
\maketitle

\section{}
\begin{enumerate}
    \item 
    \begin{enumerate}
        \item \begin{equation}
        \begin{split}
                J(w)&=\Big|Xw-y\Big|_2^2 + \lambda\Big|w\Big|_2^2\\
                J(w)&=(Xw-y)^T(Xw-y) + \lambda w^T w\\
                J(w)&=w^TX^TXw - w^TX^Ty - y^T Xw - y^Ty + \lambda w^T w\\
                J(w)&=w^TX^TXw - 2y^T Xw - y^Ty + \lambda w^T w\\
                \frac{\partial J(w)}{\partial w}&=2X^TXw -2y^TX + 2\lambda w = 0\\
        \end{split}\end{equation}
        
        then we see 
        \begin{equation}
            \begin{split}
                2X^TXw  + 2\lambda w &= 2y^TX\\
                (X^TX  + \lambda I )w &= y^TX\\
            \end{split}
        \end{equation}
        
        then we get the solution in the hw. 

        \item as \(\lambda\to 0\), \(w_{ridge}->(X^TX)^{-1}X^Ty\).  as \(\lambda\to \infty\), \(w_{ridge}->0\). 
        
        the former is when penalty goes to zero and the latter is when error dominates. 

        \item 

        \begin{equation}
            \begin{split}
                \Big| \hat X w - \hat y\Big| &= (\hat X w - \hat y)^T (\hat X w - \hat y)\\
                &= w^T\hat X^T \hat X w - \hat y^T \hat X w - w^T \hat X^T \hat y + \Big|\hat y\Big|^2\\
                &= w^T(X^T X + \sqrt{\lambda}X^TI_d + \sqrt{\lambda}I_d^TX + \lambda I) w \\
                & \quad - \hat y^T \hat X w - w^T \hat X^T \hat y + \Big|\hat y\Big|^2\\
            \end{split}
        \end{equation}

        note \((\sqrt{\lambda}Iw)^2 = \lambda\sum w_i^2\). factoring out \(w^T\), excluding the \(w=0\) solution, the argmin when zero gives the original expression.

        
    \end{enumerate}
    
    \section{}
    \begin{enumerate}
        \item \begin{equation}\begin{split}
            (X^TX+\lambda I)^{-1} &= (Q\Lambda^2Q^T + \lambda I)^{-1}\\
            &=(Q\Lambda Q^T + \lambda QIQ^T)^{-1}\\
            &=(Q(\Lambda^2 + \lambda I)Q^T)^{-1}\\
            &=(Q^T)^{-1}((\Lambda^2 + \lambda I))^{-1}Q^{-1}\\
            &=Q((\Lambda^2 + \lambda I))^{-1}Q^{T}\\
        \end{split}
        \end{equation}
        \(\lambda\) inversely scales the eigenvalue
        \item \begin{equation}
            \kappa (X^TX) = \frac{\sigma^2_{max}}{\sigma^2_min}
        \end{equation}
        since sigmas are positive, the inequality on the right holds. 

        the equality on the left is true because of part a. the eigenvalues are exactly \(\Lambda + \lambda I\)

        we can also see why equality only hods when \(\lambda\) is zero, since every eigenvalue is different on the LHS when \(\lambda\) is nonzero.

        \item if there are zero eigenvalues, it means the matrix is non invertable. the smallest eigenvalues with regularization are \(\lambda\) and no eigenvalues are zero, which implies full rank, which implies invertability. 
    \end{enumerate}

    \item \begin{enumerate}
        \item for ols
        \begin{equation}
            (X^TX)^{-1}X^Ty = X^T y
        \end{equation}

        for ridge

        \begin{equation}
            (X^TX+\lambda I)^{-1}X^T y = ((1+\lambda)I)^{-1}  X^Ty = \frac{1}{1+\lambda}X^Ty
        \end{equation}

        \item 
        using the hat symbol to denote estimate
        \begin{equation}
            \begin{split}
                E[\hat{w}_{ridge}] &= E\left[ \frac{1}{1+\lambda} X^T y \right]\\
                &= \frac{1}{1+\lambda} X^T E[y]
            \end{split}
        \end{equation}

        $$E[y] = E[Xw^* + \epsilon] = Xw^* + E[\epsilon] = Xw^*$$

        then 

        $$E[\hat{w}_{ridge}] = \frac{w^*}{1+\lambda}$$
        similarly

        \begin{equation}
            \begin{split}
                Var(\hat{w}_{ridge}) &= Var\left( \frac{1}{1+\lambda} X^T y \right)\\
                &= \left( \frac{1}{1+\lambda} \right)^2 X^T Var(y) (X^T)^T\\
                &= \frac{1}{(1+\lambda)^2} X^T Var(y) X\\
                &= \frac{\sigma^2}{(1+\lambda)^2} X^T X
            \end{split}
        \end{equation}
        \item 
        \begin{equation}\begin{split}
                \text{Bias}_j &= E[w_{ridge, j}] - w_j^*\\
                & = \frac{w_j^*}{1+\lambda} - w_j^* \\
                &= w_j^* \left( \frac{1}{1+\lambda} - 1 \right)\\
                & = w_j^* \left( \frac{1 - (1+\lambda)}{1+\lambda} \right) \\
                &= \frac{-\lambda w_j^*}{1+\lambda}\\
        \end{split}
        \end{equation}

        therefore 

        $$MSE_j = \left( \frac{\lambda w_j^*}{1+\lambda} \right)^2 + \frac{\sigma^2}{(1+\lambda)^2}$$

        to find optimal \(\lambda\), w wolfram alpha
        $$\frac{d}{d\lambda} MSE_j = \frac{2(\lambda (w_j^*)^2 - \sigma^2)}{(1+\lambda)^3}$$

        set \(\lambda\) to \(\frac{\sigma^2}{(w^*_j)^2}\) to make the numerator 0 and thus the whole term 0.

        OLS is bad when noise is high and good when noise is low. ridge is better when noise is high.
    \end{enumerate}
    \item 
    \begin{enumerate}
        \item $$P(w | X, y) = \frac{P(y | X, w) P(w)}{P(y | X)}$$
        $$\ln P(w | X, y) = \ln P(y | X, w) + \ln P(w) -\ln P(y|X)$$

        both terms are normal distributions so we see 

        $$\ln P(y | X, w) = -\frac{1}{2\sigma^2} \|y - Xw\|_2^2 + C_1$$

        $$\ln P(w) = -\frac{1}{2\tau^2} \|w\|_2^2 + C_2$$

        note, after taking log the constant normalization terms for \(P\) become additive constants.

        thus
        $$\ln P(w | X, y) = -\frac{1}{2\sigma^2} \|y - Xw\|_2^2 - \frac{1}{2\tau^2} \|w\|_2^2 + C$$

        but this is just the negative of the ridge objective up to constants so maximizing this is the same as minimizing the ridge constant obj.

        \item if \(\lambda\) is large, \(\tau^2\) is small since \(\lambda = \left( \frac{\sigma}{\tau} \right)^2\). the gaussians have small variance and are near zero. since \(\lambda\) controls the weight given to the prior by influencing \(\tau\), it exibhits similar behavior.
        
    \end{enumerate}




\end{enumerate}

\newpage
\section{}
\begin{enumerate}
    \item 
    \begin{enumerate}
        \item \begin{equation}\begin{split}
            J(w) &= (Xw - y)^T (Xw - y) + {\lambda} w^T w\\
            &=  (w^T X^T X w - w^T X^T y - y^T X w + y^T y) + {\lambda}{} w^T w\\
            &=  (w^T X^T X w - 2w^T X^T y + y^T y) + {\lambda}{} w^T w\\
        \end{split}
        \end{equation}
        \begin{equation}\begin{split}
            \nabla_w J(w)&= 2X^TX w - 2X^ty + 2\lambda w
        \end{split}
        \end{equation}
        \item see jupyter notebook
        \item 0.001 converges the fastest. 0.01 diverges. 0.001 goes fast enough but does not overshoot. 0.01 overshoots too much.
        \item see jupyter notebook

        they are not that close. 
    \end{enumerate}
    \item \begin{enumerate}
        \item if a step in the gradient descent hits exactly \(w_i=0\), the gradient of that point will be undefined which breaks the algorithm.
        \item see jupyter notebook
        \item most weights are zero. the higher hte lambda, the lower the fraction of non-zero weights
        \item lasso creates far sparser solutions. lasso does a better job of recovering nonzero entries. the force that lasso exerts on weights is constant no matter how negative or positive they are from their optimal while ridge slows down the force on weights as they near zero making it take a very long time for them to reach zero. 
    \end{enumerate}
    
\end{enumerate}

\section{}
\begin{enumerate}
    \item \begin{enumerate}
        \item lasso can be used in medicine identifying features that cause disease
        \item finance can see factors that cause changes in asset pricing
        \item in natural language processing something like this can be used to find words that are important
        \item ridge can be used by economists to find the relations between macroeconomic factors, keeping dependence on different factors without losing them competely like lasso might
    \end{enumerate}
    \item i used gemini for help w plotting
    \item gemini fast, used a different chrome tab, no copilot
    \item it took about 10-12 hours. most of the time was spent coding and verifying answers since they seemed counterintuitive
    \item i think the way that lasso behaves seems odd since the loss doesnt go to zero. also the weight differences for ridge dont go to zero either which im not sure if thats a mistake or expected. 
\end{enumerate}


\end{document}